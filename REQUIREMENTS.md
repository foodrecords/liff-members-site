# 要件定義：消費型ポイントシステムへの移行

> 作成日: 2026-06-07
> ステータス: 確定（未決事項なし）

---

## 変更概要

ポイントシステムを **達成型（累積閾値でクーポン自動発行）** から **消費型（ポイントを使ってクーポンと交換）** に再設計する。

---

## 廃止する機能

| 対象 | 内容 |
|------|------|
| 「次回の特典」タブ | 累積ポイント閾値ベースの次回報酬表示 |
| 達成型クーポン自動発行 | QRスキャン時の閾値チェック・クーポン自動付与ロジック |
| `coupon_definitions` コレクション | Firestore のクーポン定義マスタ（GASのクーポン定義シートごと廃止） |
| `/coupons` レスポンスの `upcoming` フィールド | 次回特典リスト |

---

## 新機能・変更機能

### タブ構成の変更

| 変更前 | 変更後 |
|--------|--------|
| 「次回の特典」（デフォルト） | 「獲得済みの特典」（デフォルト） |
| 「獲得済みの特典」 | 「ポイントを使う」（新規） |

### 「ポイントを使う」タブ

- 交換カタログ一覧を表示
- 各アイテムに **必要ポイント** と **交換ボタン** を表示
- 現在のポイント残高が不足している場合はボタンをグレーアウト
- 交換ボタンタップ → 確認ダイアログ → APIコール → 成功時にトースト表示・残高更新

### 交換カタログの初期内容（スプレッドシートで変更可能）

| 必要ポイント | 特典名 |
|-------------|--------|
| 200pt | 基本トッピングチケット |
| 400pt | 豪華トッピングチケット |
| 750pt | おかずチケット |
| 1200pt | お弁当チケット |

---

## 設計決定事項

### `members.point` の意味の変更

| 現行 | 変更後 |
|------|--------|
| 累積獲得ポイント（減らない） | 現在残高（交換で減る） |

### ランクシステム：案B採用（累積ポイントで維持）

`members` に `total_earned_point`（累積獲得ポイント）フィールドを追加。
ランク計算は `total_earned_point` を参照し、ランクダウンなしを維持。
`point`（残高）は交換で減算されるが、`total_earned_point` は減らない。

| フィールド | 役割 |
|-----------|------|
| `point` | 現在残高（交換で減算） |
| `total_earned_point` | 累積獲得ポイント（ランク計算・ランクダウンなし） |

---

## Firestoreデータモデル変更

### `members` コレクション（変更）

| フィールド | 変更内容 |
|-----------|---------|
| `point` | 意味が変わる：**現在残高**（交換で減算） |
| `total_earned_point` | 【新規】累積獲得ポイント（ランク計算に使用・減算しない） |
| `rank` | `total_earned_point` 基準で更新（ランクダウンなし維持） |

### `coupon_definitions` コレクション（廃止）

既存データはそのまま残しつつ新規書き込みを停止（アーカイブ）。
`member_coupons` への既存参照は互換性のために維持。

### `reward_catalog/{id}` （新規）

スプレッドシートから GAS で同期する交換カタログ。

| フィールド | 型 | 説明 |
|-----------|---|------|
| `title` | string | 特典名（例: 基本トッピングチケット） |
| `required_points` | int | 必要ポイント（例: 200） |
| `description` | string | 説明文（利用方法など） |
| `active` | bool | 有効/無効 |
| `image_url` | string | 画像URL |
| `sort_order` | int | 表示順 |

### `member_coupons/{couponCode}` （変更）

| フィールド | 変更内容 |
|-----------|---------|
| `point_milestone` | 廃止 |
| `reward_id` | 【新規】`reward_catalog` のドキュメントID |
| `point_cost` | 【新規】交換時に消費したポイント数 |
| `issued_at`, `used`, `used_at` | 変更なし |
| `member_id`, `discount_amount` | 変更なし |

---

## APIエンドポイント変更

### 変更

| メソッド | パス | 変更内容 |
|---------|------|---------|
| `GET` | `/coupons` | レスポンスから `upcoming` を削除 |
| `POST` | `/qrcode` | クーポン自動発行ロジックを削除（`new_coupons` フィールドも削除） |
| `GET` | `/members` | `total_earned_point` を追加 |

### 新規

| メソッド | パス | 説明 |
|---------|------|------|
| `GET` | `/rewards` | 交換カタログ一覧（`active: true` のみ、`sort_order` 昇順） |
| `POST` | `/rewards/{id}/exchange` | ポイントを消費してクーポンを発行 |

#### `POST /rewards/{id}/exchange` の処理

```
1. メンバーの現在残高を確認
2. 残高 < required_points なら 400 エラー（INSUFFICIENT_POINTS）
3. reward_catalog/{id} を取得（active: true であることを確認）
4. Firestore トランザクション:
   a. members/{userId}.point -= required_points
   b. total_earned_point は変更しない
   c. member_coupons/{newCode} にクーポン発行
5. 発行したクーポン情報を返却
```

---

## フロントエンド変更

### タブ変更

```
変更前:
  [次回の特典] [獲得済みの特典]

変更後:
  [獲得済みの特典] [ポイントを使う]
```

### 「ポイントを使う」タブの UI

- カタログアイテムをカード形式で一覧表示
- 各カード: 画像・タイトル・必要ポイント・交換ボタン
- 残高不足のアイテムはボタンを `disabled` 表示（「残高不足」テキスト）
- 交換後は「獲得済みの特典」タブへ自動スクロール（現行のクーポン獲得トーストを流用）

### カードポイント表示

- 「現在のポイント」の数値はそのまま表示（ただし意味が「残高」に変わる）
- ランクバッジとプログレスチャートは `total_earned_point` を参照

### 削除する UI

- `renderUpcoming()` 関数と関連要素
- `buildUpcomingCard()` 関数
- QRスキャン時のクーポン獲得トースト（`showCouponToast`）
- `openUpcomingModal()` 関数

---

## GAS（スプレッドシート）変更

### 廃止

- 「クーポン定義」シートおよび `syncCouponDefinitions()` / `loadCouponDefinitions()` / `createCouponSheet()` 関数

### 新規追加

#### 「交換特典」シート

| 列 | 内容 | 例 |
|----|------|---|
| A | 定義ID | `topping_basic` |
| B | タイトル | 基本トッピングチケット |
| C | 必要ポイント | 200 |
| D | 説明文 | 店舗スタッフにこの画面をご提示ください |
| E | 有効 | TRUE |
| F | 画像URL | https://... |
| G | 表示順 | 1 |

#### 新規関数

| 関数名 | 機能 |
|--------|------|
| `createRewardCatalogSheet()` | 「交換特典」シートを初期データ付きで作成 |
| `syncRewardCatalog()` | シート → Firestore `reward_catalog` に同期 |
| `loadRewardCatalog()` | Firestore → シートに読み込み |

#### メニュー変更

```
変更前:
  シリアル管理
    > シリアルナンバー生成
    > クーポン定義を読み込み
    > クーポン定義を同期
    > クーポン定義シートを作成

変更後:
  シリアル管理
    > シリアルナンバー生成
    > 交換特典を読み込み（Firestore → シート）
    > 交換特典を同期（シート → Firestore）
    > 交換特典シートを作成
```

---

## 実装順序（案）

1. **Firestore スキーマ確認・移行方針決定**（未決事項の確認）
2. **GAS: 交換特典シート実装** → `reward_catalog` へ同期
3. **API: `GET /rewards` 実装**
4. **API: `POST /rewards/{id}/exchange` 実装**
5. **API: `/coupons` から `upcoming` 削除**
6. **API: `/qrcode` から自動クーポン発行ロジック削除**
7. **Frontend: タブ変更・「ポイントを使う」UI実装**
8. **Frontend: 不要な `upcoming` 関連コード削除**
9. **devserver の対応（ローカル開発用）**
10. **CLAUDE.md 更新**

---

## 後方互換・移行

- **既存の `member_coupons`（達成型で発行済みクーポン）は引き続き「獲得済みの特典」に表示する**（`reward_id` がなくても表示できるよう API で互換対応）
- `coupon_definitions` コレクションへの書き込みは停止するが、読み取りはしばらく維持
